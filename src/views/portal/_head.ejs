<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= typeof pageTitle !== 'undefined' && pageTitle ? pageTitle + ' - ' : '' %>Soundtrack Prayer Times</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
  <script src="https://unpkg.com/htmx.org@2.0.4"></script>
  <link rel="stylesheet" href="/public/style.css">
</head>
<body>
  <nav class="site-nav">
    <div class="site-nav__inner">
      <a href="<%= basePath %>" class="site-nav__brand">
        <img src="/public/BMAsia_Logo.png" alt="BMAsia" class="site-nav__logo">
      </a>
      <ul class="site-nav__links">
        <li><a href="<%= basePath %>"<%= typeof currentPage !== 'undefined' && currentPage === 'dashboard' ? ' class="is-active"' : '' %>>Dashboard</a></li>
        <li><a href="<%= basePath %>/zones/new"<%= typeof currentPage !== 'undefined' && currentPage === 'zones-new' ? ' class="is-active"' : '' %>>Add Zone</a></li>
        <li><a href="<%= basePath %>/log"<%= typeof currentPage !== 'undefined' && currentPage === 'log' ? ' class="is-active"' : '' %>>Activity Log</a></li>
        <li><span class="text-muted" style="font-size: var(--font-size-xs); padding: var(--space-2);"><%= customer.name %></span></li>
      </ul>
    </div>
  </nav>
  <main class="page-content">
    <div class="container">
