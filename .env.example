# Soundtrack API (same token as soundtrack-mcp)
SOUNDTRACK_API_TOKEN=your_base64_encoded_token_here

# PostgreSQL (Render provides this as DATABASE_URL)
DATABASE_URL=postgresql://user:password@localhost:5432/prayertimes

# Admin password for web UI
ADMIN_PASSWORD=changeme

# Server
PORT=3000
